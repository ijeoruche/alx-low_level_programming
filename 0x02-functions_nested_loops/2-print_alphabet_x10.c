#include "main.h"

/**
 * print_alphabet_x10.c - prints alpha 10 times
 */
void print_alphabet_x10.c(void)
{
	int ten;
	char la;

	for (ten = 0; ten <= 9; ten++)
	{
		for (la = 'a'; la <= 'z'; la++)
			_putchar(la);
		-putchar('\n');
	}
}
